name: count
runtime: nodejs
description: AWS Count Example
template:
  description: A minimal AWS TypeScript Pulumi program
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
